dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

include ':module', ':apk_for_test', ':hiddenapi:stubs', ':hiddenapi:bridge'
